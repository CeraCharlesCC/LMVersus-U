<configuration>
    <property name="LOG_LEVEL_ROOT" value="${lmversusu.root.log.level:-INFO}"/>
    <property name="LOG_LEVEL_LMV" value="${lmversusu.app.log.level:-INFO}"/>
    <property name="LOG_PATTERN" value="${lmversusu.log.format:-%d{YYYY-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n}"/>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <root level="${LOG_LEVEL_ROOT:-INFO}">
        <appender-ref ref="STDOUT"/>
    </root>
    <logger name="LMVersus-U" level="${LOG_LEVEL_LMV}"/>

    <logger name="org.eclipse.jetty" level="INFO"/>
    <logger name="io.netty" level="INFO"/>
</configuration>
